# =============================================================================
# EB-YOLO — full-project Python dependencies
#
# Includes:
#   - Ultralytics upstream runtime + optional extras (from EB-YOLO-main/info/pyproject.toml)
#   - EB-YOLO fork extensions (extra backbones / heads / tools under EB-YOLO-main/main)
#   - Root-level utilities (dataset/, pre-processing/, yaml workflows)
#
# Python: >= 3.8
#
# 1) Install PyTorch for your CUDA / CPU:
#    https://pytorch.org/get-started/locally/
#    On Windows CPU, avoid torch==2.4.0 (Ultralytics issue #15049).
#
# 2) Install dependencies, then the local package:
#    pip install -r requirements.txt
#    pip install -e EB-YOLO-main/info
#
# 3) Mamba / Triton stacks are optional and often need Linux + matching CUDA toolchains.
#    See the OPTIONAL section at the bottom before pip-installing everything on Windows.
# =============================================================================

# --- Build / editable install ---
setuptools>=70.0.0
wheel

# =============================================================================
# --- A. Ultralytics core runtime (pyproject [project] dependencies) ---
# =============================================================================
numpy>=1.23.0
matplotlib>=3.3.0
opencv-python>=4.6.0
pillow>=7.1.2
pyyaml>=5.3.1
requests>=2.23.0
scipy>=1.4.1
torch>=1.8.0
torchvision>=0.9.0
tqdm>=4.64.0
psutil
py-cpuinfo
pandas>=1.1.4
seaborn>=0.11.0
ultralytics-thop>=2.0.0

# Windows: torch 2.4.0 has known CPU issues
torch!=2.4.0; sys_platform == "win32"

# =============================================================================
# --- B. EB-YOLO fork — extended models & tools (not in stock ultralytics wheel)
#     Used by main/nn/tasks.py, extra_modules/, backbones/, etc.
# =============================================================================
timm>=0.9.0
einops>=0.6.0
efficientnet-pytorch>=0.7.1
PyWavelets>=1.4.0

# Weight conversion (e.g. convert_convnextv2_safetensor.py)
safetensors>=0.4.0

# =============================================================================
# --- C. Interpretability YOLO (EB-YOLO-main/Interpretability YOLO/)
# =============================================================================
pytorch-grad-cam>=0.2.1

# =============================================================================
# --- D. Ultralytics optional groups (same as upstream pyproject extras) ---
# =============================================================================

# --- [dev] — docs & tests ---
ipython
pytest
pytest-cov
coverage[toml]
mkdocs>=1.6.0
mkdocs-material>=9.5.9
mkdocstrings[python]
mkdocs-jupyter
mkdocs-redirects
mkdocs-ultralytics-plugin>=0.1.8
mkdocs-macros-plugin>=1.0.5
beautifulsoup4>=4.12.0

# --- [export] ---
onnx>=1.12.0
coremltools>=7.0; platform_system != "Windows" and python_version <= "3.11"
scikit-learn>=1.3.2; platform_system != "Windows" and python_version <= "3.11"
openvino>=2024.0.0
tensorflow>=2.0.0
tensorflowjs>=3.9.0
tensorstore>=0.1.63; platform_machine == "aarch64" and python_version >= "3.9"
keras
flatbuffers>=23.5.26,<100; platform_machine == "aarch64"
numpy==1.23.5; platform_machine == "aarch64"
h5py!=3.11.0; platform_machine == "aarch64"

# --- [explorer] ---
lancedb
duckdb<=0.9.2
streamlit

# --- [logging] ---
comet
tensorboard>=2.13.0
dvclive>=2.12.0

# --- [extra] ---
hub-sdk>=0.0.12
albumentations>=1.4.6
pycocotools>=2.0.7

# =============================================================================
# --- E. OPTIONAL — Mamba / selective-scan / Triton (only if you use those modules)
#
# Many YAMLs under yaml/ do NOT need these. Install only when imports fail, on a
# Linux + CUDA setup compatible with your torch version. Typical packages:
#
#   pip install mamba-ssm causal-conv1d
#   # triton: often provided by PyTorch nightly / separate wheel; lsnet may import it
#
# Do not uncomment blindly on Windows — builds frequently fail without MSVC + CUDA.
# =============================================================================
# mamba-ssm
# causal-conv1d
# triton

# Triton *inference server* client (main/utils/triton.py) — deployment only
# tritonclient[http]>=2.34.0
